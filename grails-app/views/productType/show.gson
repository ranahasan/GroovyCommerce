import dev.matija.groovycommerce.ProductType

model {
    ProductType productType
}

json {
    hal.links(productType)
    id productType.id
    typeName productType.typeName
    isDeleted productType.isDeleted
    companyId productType.companyId
}